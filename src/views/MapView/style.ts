import styled from 'styled-components';

export const MapViewContainer = styled.div`
    position: relative;
    width: 100%;
    height: 100%;
`;

export const MapViewCanvas = styled.canvas`
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
`;
